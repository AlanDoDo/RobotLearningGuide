创建新的软件包

```
cd catkin_ws/src/
```

```
catkin_create_pkg atr_pkg rospy roscpp std_msgs
```

在src下创建ma_node.cpp，编写代码

sub = nh.subscribe("话题名称",缓存长度,回调函数);
```cpp
#include "ros/ros.h"
#include <std_msgs/String.h>

void chao_callback(std_msgs::String msg)
{
    ROS_INFO(msg.data.c_str());
}

void yao_callback(std_msgs::String msg)
{
    ROS_WARN(msg.data.c_str());
}

int main(int argc, char *argv[])
{
    setlocale(LC_ALL,"zh_CN.UTF-8");
    //执行 ros 节点初始化
    ros::init(argc,argv,"ma_node");
    //创建 ros 节点句柄 understand a helper
    ros::NodeHandle nh;
    ros::Subscriber sub = nh.subscribe("player_chao",10,chao_callback);
    ros::Subscriber sub_2 = nh.subscribe("player_yao",10,yao_callback);

    //控制台输出 


    while(ros::ok())
    {
        ros::spinOnce();
    }

    return 0;
}
```

编写Cmake
```cmake
add_executable(ma_node src/ma_node.cpp)

target_link_libraries(ma_node
  ${catkin_LIBRARIES}
)
```

编译，运行roscore，运行
```
rosrun ssr_pkg chao_node
```

```
rosrun ssr_pkg yao_node
```

运行
```
rosrun atr_pkg ma_node
```

可视化节点，图形化显示当前系统活跃的节点以及节点间的话题通讯关系
```
rqt_graph
```

话题的订阅

1. 确定*话题名称*和*消息类型*
2. 在代码文件中 include<ros.h> 和*消息类型*对应的*头文件*
3. 在main函数中通过*NodeHandler*大管家订一个话题并设置消息接收*回调函数*
4. 定义一个*回调函数*，对接收到的*消息包*进行处理
5. main函数中需要执行*ros::spinOnce()*，让回调函数能够响应接收到的消息包
## Python实现

在atr_pkg下创建scripts文件夹，然后创建文件ma_node_py.py

编写代码
```python
#!/usr/bin/env python3
#coding=utf-8

import rospy
from std_msgs.msg import String

def chao_callback(msg):
    rospy.loginfo(msg.data)

def yao_callback(msg):
    rospy.logwarn(msg.data)



if __name__ == "__main__":
    rospy.init_node("ma_node_py")
    rospy.logwarn("Let's go my bro! --ma")

    sub = rospy.Subscriber("kai_hei_qun",String,chao_callback,queue_size=10)
    sub = rospy.Subscriber("kai_hei_qun_yao",String,yao_callback,queue_size=10)
    
    rospy.spin()
```

添加权限
```
chmod +x ma_node_py.py 
```

编译，运行roscore，运行发布
```
rosrun ssr_pkg chao_node_py.py
```

```
rosrun ssr_pkg yao_node_py.py
```

订阅
```
rosrun atr_pkg ma_node_py.py
```