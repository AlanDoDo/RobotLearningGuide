## Rviz观测传感器数据

启动roscore，执行
```
roslaunch wpr_simulation wpb_simple.launch 
```

打开rviz
```
rviz
```

添加机器人模型，坐标系修改为`base_footprint`

添加一条LaserScan，修改订阅Topic为`/scan`

可以看到一条红点

关闭rviz启动
```
roslaunch wpr_simulation wpb_rviz.launch
```

## 激光雷达消息包格式

启动，查看消息包数据
```
rostopic echo /scan --noarr
```

## C++获取激光雷达的测距数值

wpr_simulation中有个例程程序
```
roslaunch wpr_simulation wpb_simple.launch 
```

```
rosrun wpr_simulation demo_lidar_data
```

构思实现
1. 构建一个新的软件包，包名叫做lidar_pkg。
2. 在软件包中新建一个节点，节点名叫做lidar_node。
3. 在节点中，向ROS大管家NodeHandle申请订阅话题`/scan`，并设置回调函数为LidarCallback()。
4. 构建回调函数LidarCallback()，用来接收和处理雷达数据。
5. 调用ROS_INFO()显示雷达检测到的前方障碍物距离

构建功能包
```
catkin_create_pkg lidar_pkg roscpp rospy sensor_msgs
```

进入src新建lidar_node.cpp
```cpp
#include <ros/ros.h>
#include <sensor_msgs/LaserScan.h>

void LidarCallback(const sensor_msgs::LaserScan msg)
{
    float fMidDist = msg.ranges[180];
    ROS_INFO("Forward_ranging ranges[180] = %f meter", fMidDist);
}

int main(int argc, char *argv[])
{
    setlocale(LC_ALL,"");
    ros::init(argc, argv, "lidar_node");

    ros::NodeHandle n;
    ros::Subscriber lidar_sub = n.subscribe("/scan", 10, &LidarCallback);

    ros::spin();

    return 0;
}
```

构建CMake
```c
add_executable(lidar_node src/lidar_node.cpp)

add_dependencies(lidar_node ${${PROJECT_NAME}_EXPORTED_TARGETS} ${catkin_EXPORTED_TARGETS})

target_link_libraries(lidar_node
  ${catkin_LIBRARIES}
)
```

运行roscore

```
roslaunch wpr_simulation wpb_simple.launch 
```

```
rosrun lidar_pkg lidar_node
```

## Python实现

在lidar_pkg功能包下创建scripts文件夹，创建节点*lidar_py_node.py*

```python
#!/usr/bin/python3
# -*- coding: utf-8 -*-

import rospy
from sensor_msgs.msg import LaserScan

def LidarCallback(msg):
    dist = msg.ranges[180]
    rospy.loginfo("Forward_ranging ranges[180] = %f meter", dist)

if __name__ == "__main__":
    rospy.init_node("lidar_py_node")

    lidar_sub = rospy.Subscriber("/scan", LaserScan, LidarCallback, queue_size=10)

    rospy.spin()
```

添加执行权限
```
chmod +x lidar_py_node.py
```

启动roscore，执行
```
roslaunch wpr_simulation wpb_simple.launch 
```

```
rosrun lidar_pkg lidar_py_node
```

## 用C++实现雷达避障

实现步骤
1. 让大管家 NodeHandle 发布速度控制话题 `/cmd_vel`。
2. 构建速度控制消息包 `vel_cmd`。
3. 根据激光雷达的测距数值，实时调整机器人运动速度，避开障碍物。

```cpp
#include <ros/ros.h>
#include <sensor_msgs/LaserScan.h>
#include <geometry_msgs/Twist.h>

ros::Publisher vel_pub;
int nCount = 0;

void LidarCallback(const sensor_msgs::LaserScan msg)
{
    float fMidDist = msg.ranges[180];
    ROS_INFO("Forward_ranging ranges[180] = %f meter", fMidDist);

    if(nCount > 0)
    {
        nCount--;
        return;
    }

    geometry_msgs::Twist vel_cmd;
    if(fMidDist < 1.5)
    {
        vel_cmd.angular.z = 0.3;
        nCount = 50;
    }
    else
    {
        vel_cmd.linear.x = 0.1;
    }
    vel_pub.publish(vel_cmd);
}

int main(int argc, char *argv[])
{
    setlocale(LC_ALL,"");
    ros::init(argc, argv, "lidar_node");

    ros::NodeHandle n;
    ros::Subscriber lidar_sub = n.subscribe("/scan", 10, &LidarCallback);
    vel_pub = n.advertise<geometry_msgs::Twist>("/cmd_vel",10);

    ros::spin();

    return 0;
}
```

启动roscore，执行
```
roslaunch wpr_simulation wpb_simple.launch 
```

```
rosrun lidar_pkg lidar_node
```

## 用Python实现雷达避障

## Python实现

```python
#!/usr/bin/python3
# -*- coding: utf-8 -*-

import rospy
from sensor_msgs.msg import LaserScan
from geometry_msgs.msg import Twist

count = 0

def LidarCallback(msg):
    global vel_pub
    global count
    dist = msg.ranges[180]
    rospy.loginfo("Forward_ranging ranges[180] = %f meter", dist)
    
    if count > 0:
        count = count - 1
        return
    
    vel_cmd = Twist()
    if dist < 1.5:
        vel_cmd.angular.z = 0.3
        count = 50
    else:
        vel_cmd.linear.x = 0.05
    vel_pub.publish(vel_cmd)

if __name__ == "__main__":
    rospy.init_node("lidar_py_node")

    lidar_sub = rospy.Subscriber("/scan", LaserScan, LidarCallback, queue_size=10)
    vel_pub = rospy.Publisher("/cmd_vel", Twist, queue_size=10)

    rospy.spin()
```

添加执行权限
```
chmod +x lidar_py_node.py
```

启动roscore，执行
```
roslaunch wpr_simulation wpb_simple.launch 
```

```
rosrun lidar_pkg lidar_py_node.py
```