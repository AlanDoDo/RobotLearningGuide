
在chao_node.cpp中编写代码
```cpp
#include "ros/ros.h"
#include <std_msgs/String.h>

int main(int argc, char *argv[])
{
    //执行 ros 节点初始化
    ros::init(argc,argv,"chao_node");
    //创建 ros 节点句柄 understand a helper
    ros::NodeHandle nh;
    ros::Publisher pub = nh.advertise<std_msgs::String>("player_chao",10);

    ros::Rate loop_rate(10);
    //控制台输出 


    while(ros::ok())
    {
        printf("let's go!\n");
        std_msgs::String msg; // Generate a message package
        msg.data = "Good player!";
        pub.publish(msg);
        loop_rate.sleep();
    }

    return 0;
}
```

nh.advertise("话题名称",*缓存长度*); //构建消息发送对象
缓存长度：表示缓存里面可以容纳多少个消息包排队

编译后启动终端，启动roscore，运行
```
rosrun ssr_pkg chao_node 
```

rostopic查看

```
rostopic list 
```

```
rostopic echo /player_chao
```

查看循环频率
```
rostopic hz /play
```

## 总结

1. 确定*话题名称*和*消息类型*。
2. 在代码文件中include*消息类型*对应的*头文件*。
3. 在main函数中*通过NodeHandler*大管家发布一个话题并得到消息发送对象。
4. *生成*要发送的*消息包*并进行发送数据的*赋值*。
5. 调用消息发送对象的*publish()函数*将消息包*发送*到话题当中。

## 常用工具

* rostopic list   列出当前系统中所有活跃着的话题。
* rostopic echo **主题名称**   显示指定话题中发送的消息包内容。
* rostopic hz **主题名称**   统计指定话题中消息包发送频率。

## 复制一个节点

复制chao_node.cpp，修改为yao_node.cpp，粘贴代码

```cpp
#include "ros/ros.h"
#include <std_msgs/String.h>

int main(int argc, char *argv[])
{
    //执行 ros 节点初始化
    ros::init(argc,argv,"chao_node");
    //创建 ros 节点句柄 understand a helper
    ros::NodeHandle nh;
    ros::Publisher pub = nh.advertise<std_msgs::String>("player_chao",10);

    ros::Rate loop_rate(10);
    //控制台输出 


    while(ros::ok())
    {
        printf("let's go!\n");
        std_msgs::String msg; // Generate a message package
        msg.data = "Good player!";
        pub.publish(msg);
        loop_rate.sleep();
    }

    return 0;
}
```

修改CMakeLists.txt添加
```cmake
add_executable(yao_node src/yao_node.cpp)

target_link_libraries(yao_node
  ${catkin_LIBRARIES}
)
```

编译后运行roscore，运行

```
rostopic list 
```

```
rostopic echo /player_yao
```


## Python实现

在 ssr_pkg 文件下创建 scripts 文件夹，创建 chao_node_py.py 文件

编写python文件
```python
#!/usr/bin/env python3
#coding=utf-8

import rospy
from std_msgs.msg import String

if __name__ == "__main__":
    rospy.init_node("chao_node_py")
    rospy.logwarn("Let's go my bro!")

    pub = rospy.Publisher("kai_hei_qun",String,queue_size=10)

    rate = rospy.Rate(10)

    while not rospy.is_shutdown():
        rospy.loginfo("let's go")
        msg = String()
        msg.data = "I will carry you!"
        pub.publish(msg)
        rate.sleep()
```

打开这个终端增加权限

```
chmod +x chao_node_py.py
```

运行roscore，运行

```
rosrun ssr_pkg chao_node_py.py
```


```python
#!/usr/bin/env python3
#coding=utf-8

import rospy
from std_msgs.msg import String

if __name__ == "__main__":
    rospy.init_node("yao_node_py")
    rospy.logwarn("Let's go my bro! --yao")

    pub = rospy.Publisher("kai_hei_qun_yao",String,queue_size=10)

    rate = rospy.Rate(10)

    while not rospy.is_shutdown():
        rospy.loginfo("let's go")
        msg = String()
        msg.data = "I will carry you! yao"
        pub.publish(msg)
        rate.sleep()
```

添加可执行权限，这里不添加会找不到路径
```
chmod +x yao_node_py.py
```

运行roscore，运行

```
rosrun ssr_pkg yao_node_py.py
```